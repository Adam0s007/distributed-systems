# Quick Start Guide

## Setup Instructions

1. **Install Dependencies**  
   In your terminal run:
   ```bash
   npm install
   ```
2. **Start the Server**

    Run the server with:
    ```bash
    node server.js
    ```
